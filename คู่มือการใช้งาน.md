# 🎵 คู่มือการติดตั้งและใช้งาน Audio File Manager

## 📋 เกี่ยวกับโปรแกรม

Audio File Manager เป็นโปรแกรมจัดการไฟล์เสียงที่มีหน้าต่างกราฟิก (GUI) ช่วยให้คุณสามารถ:
- 📁 จัดระเบียบไฟล์เสียงตามโฟลเดอร์
- 🎧 รวมไฟล์เสียงหลายไฟล์เป็นไฟล์เดียว
- 🔄 สร้างไฟล์เสียงแบบวนซ้ำ (Loop)

---

## 🔧 การติดตั้ง

### ความต้องการของระบบ

#### สำหรับ Windows:
- Windows 7 หรือสูงกว่า
- Python 3.6 ขึ้นไป

#### สำหรับ macOS/Linux:
- Python 3.6 ขึ้นไป
- FFmpeg (สำหรับการประมวลผลเสียง)

### ขั้นตอนการติดตั้ง

#### 1. ติดตั้ง Python

**Windows:**
1. ดาวน์โหลด Python จาก https://www.python.org/downloads/
2. เปิดไฟล์ติดตั้ง
3. ✅ **สำคัญ:** ติ๊กถูกที่ "Add Python to PATH"
4. คลิก "Install Now"
5. รอจนติดตั้งเสร็จและคลิก "Close"

**macOS:**
```bash
# ใช้ Homebrew
brew install python3
```

**Linux (Ubuntu/Debian):**
```bash
sudo apt update
sudo apt install python3 python3-pip
```

#### 2. ติดตั้ง FFmpeg (จำเป็นสำหรับการประมวลผลเสียง)

**Windows:**
1. ดาวน์โหลด FFmpeg จาก https://www.gyan.dev/ffmpeg/builds/
2. แตกไฟล์ไปที่ `C:\ffmpeg`
3. เพิ่ม `C:\ffmpeg\bin` เข้าไปใน System PATH:
   - คลิกขวาที่ "This PC" > Properties
   - คลิก "Advanced system settings"
   - คลิก "Environment Variables"
   - เลือก "Path" แล้วคลิก "Edit"
   - คลิก "New" แล้วใส่ `C:\ffmpeg\bin`
   - คลิก "OK" ทุกหน้าต่าง

**macOS:**
```bash
brew install ffmpeg
```

**Linux (Ubuntu/Debian):**
```bash
sudo apt install ffmpeg
```

#### 3. ติดตั้ง Library ที่จำเป็น

เปิด Command Prompt (Windows) หรือ Terminal (macOS/Linux) แล้วรันคำสั่ง:

```bash
pip install pydub
```

หรือถ้าใช้ Python 3:
```bash
pip3 install pydub
```

**หมายเหตุ:** tkinter มักจะติดตั้งมาพร้อม Python อยู่แล้ว ถ้ายังไม่มี:

**Linux:**
```bash
sudo apt install python3-tk
```

---

## 🚀 วิธีเปิดโปรแกรม

### วิธีที่ 1: ใช้ไฟล์ Launch Script (แนะนำ)

#### Windows:
1. ดับเบิลคลิกที่ไฟล์ `launch_audio_manager.bat`
2. โปรแกรมจะเปิดขึ้นมาทันที

#### macOS/Linux:
1. เปิด Terminal
2. ไปที่โฟลเดอร์โปรแกรม:
   ```bash
   cd "D:\Vibe Code\mixpro1.0"
   ```
3. รันคำสั่ง:
   ```bash
   chmod +x launch_audio_manager.sh
   ./launch_audio_manager.sh
   ```

### วิธีที่ 2: ใช้คำสั่ง Python โดยตรง

1. เปิด Command Prompt หรือ Terminal
2. ไปที่โฟลเดอร์โปรแกรม:
   ```bash
   cd "D:\Vibe Code\mixpro1.0"
   ```
3. รันคำสั่ง:
   ```bash
   python audio_manager.py
   ```
   หรือ
   ```bash
   python3 audio_manager.py
   ```

---

## 📖 วิธีใช้งาน

เมื่อเปิดโปรแกรมขึ้นมา คุณจะเห็นหน้าต่างหลักที่มี 3 โหมดการทำงาน:

### 📁 โหมดที่ 1: จัดระเบียบไฟล์เสียง

โหมดนี้ใช้สำหรับจัดกลุ่มไฟล์เสียงที่มีชื่อคล้ายกันไปไว้ในโฟลเดอร์เดียวกัน

**ขั้นตอนการใช้งาน:**

1. คลิกแท็บ **"📁 จัดระเบียบไฟล์"** (ด้านบนสุด)
2. คลิกปุ่ม **"เลือกไฟล์เสียง"**
3. เลือกไฟล์เสียงที่ต้องการจัดระเบียบ (สามารถเลือกหลายไฟล์พร้อมกันได้)
4. โปรแกรมจะแสดงตัวอย่างการจัดกลุ่มไฟล์
5. ตรวจสอบว่าการจัดกลุ่มถูกต้องหรือไม่
6. คลิกปุ่ม **"จัดระเบียบไฟล์"**
7. เลือกโฟลเดอร์ปลายทางที่ต้องการบันทึก
8. รอจนกว่าโปรแกรมจะประมวลผลเสร็จ

**ตัวอย่าง:**
```
ไฟล์ต้นทาง:
- song_01.mp3
- song_02.mp3
- music_01.mp3
- music_02.mp3

ผลลัพธ์:
📁 song/
  - song_01.mp3
  - song_02.mp3
📁 music/
  - music_01.mp3
  - music_02.mp3
```

---

### 🎧 โหมดที่ 2: รวมไฟล์เสียง

โหมดนี้ใช้สำหรับรวมไฟล์เสียงหลายไฟล์ในแต่ละโฟลเดอร์ให้เป็นไฟล์เดียว

**ขั้นตอนการใช้งาน:**

1. คลิกแท็บ **"🎧 รวมไฟล์เสียง"**
2. คลิกปุ่ม **"เลือกโฟลเดอร์"**
3. เลือกโฟลเดอร์ที่มีไฟล์เสียงที่ต้องการรวม (เลือกได้หลายโฟลเดอร์)
4. โปรแกรมจะแสดงรายการไฟล์ที่จะถูกรวมในแต่ละโฟลเดอร์
5. คลิกปุ่ม **"รวมไฟล์เสียง"**
6. รอจนกว่าโปรแกรมจะประมวลผลเสร็จ
7. ไฟล์ที่รวมแล้วจะถูกบันทึกในโฟลเดอร์เดิมโดยอัตโนมัติ

**ตัวอย่าง:**
```
โฟลเดอร์ต้นทาง:
📁 Chapter_01/
  - part1.mp3 (5 นาที)
  - part2.mp3 (3 นาที)
  - part3.mp3 (4 นาที)

ผลลัพธ์:
📁 Chapter_01/
  - Chapter_01_merged.mp3 (12 นาที)
  - part1.mp3
  - part2.mp3
  - part3.mp3
```

---

### 🔄 โหมดที่ 3: ลูปเสียง

โหมดนี้ใช้สำหรับสร้างไฟล์เสียงที่วนซ้ำตามจำนวนครั้งที่กำหนด

**ขั้นตอนการใช้งาน:**

1. คลิกแท็บ **"🔄 ลูปเสียง"**
2. คลิกปุ่ม **"เลือกไฟล์เสียง"**
3. เลือกไฟล์เสียงที่ต้องการทำการลูป (เลือกได้หลายไฟล์)
4. โปรแกรมจะแสดงรายการไฟล์ที่เลือก
5. กำหนดจำนวนครั้งที่ต้องการลูป (ค่าเริ่มต้น: 2 ครั้ง)
6. คลิกปุ่ม **"สร้างไฟล์ลูป"**
7. เลือกโฟลเดอร์ปลายทางที่ต้องการบันทึก
8. รอจนกว่าโปรแกรมจะประมวลผลเสร็จ

**ตัวอย่าง:**
```
ไฟล์ต้นทาง:
- background_music.mp3 (1 นาที)

ตั้งค่า: ลูป 3 ครั้ง

ผลลัพธ์:
- background_music_loop_3x.mp3 (3 นาที)
  [เสียงเดิมเล่นซ้ำ 3 รอบติดกัน]
```

---

## 🎯 เคล็ดลับการใช้งาน

### สำหรับการจัดระเบียบไฟล์:
- ✅ ตั้งชื่อไฟล์ให้มีรูปแบบที่ชัดเจน เช่น `song_01.mp3`, `song_02.mp3`
- ✅ ใช้ underscore (_) หรือ hyphen (-) แยกส่วนต่างๆ ของชื่อไฟล์
- ✅ ตรวจสอบตัวอย่างก่อนคลิก "จัดระเบียบไฟล์"

### สำหรับการรวมไฟล์เสียง:
- ✅ ไฟล์เสียงในโฟลเดอร์เดียวกันควรเป็นประเภทเดียวกัน (MP3, WAV, etc.)
- ✅ ตรวจสอบว่าไฟล์เรียงลำดับถูกต้องแล้ว (เรียงตามชื่อไฟล์)
- ⚠️ การรวมไฟล์ขนาดใหญ่อาจใช้เวลานาน

### สำหรับการลูปเสียง:
- ✅ เหมาะสำหรับเสียงพื้นหลังหรือเพลงสั้นๆ
- ✅ ระวังขนาดไฟล์ขาออก (ลูป 10 ครั้ง = ขนาดไฟล์เพิ่มขึ้น 10 เท่า)
- ✅ แนะนำให้ลูปไม่เกิน 5-10 ครั้งเพื่อไม่ให้ไฟล์ใหญ่เกินไป

---

## ⚠️ การแก้ปัญหาที่พบบ่อย

### ปัญหา: โปรแกรมเปิดไม่ขึ้น

**วิธีแก้:**
1. ตรวจสอบว่าติดตั้ง Python แล้วหรือยัง:
   ```bash
   python --version
   ```
2. ตรวจสอบว่าติดตั้ง pydub แล้วหรือยัง:
   ```bash
   pip list | grep pydub
   ```
3. ลองเปิดด้วย Command Prompt/Terminal โดยตรงเพื่อดู error message

### ปัญหา: ไม่สามารถรวมไฟล์เสียงได้

**วิธีแก้:**
1. ตรวจสอบว่าติดตั้ง FFmpeg แล้วหรือยัง:
   ```bash
   ffmpeg -version
   ```
2. ถ้ายังไม่มี ให้ติดตั้งตามขั้นตอนด้านบน
3. ตรวจสอบว่าไฟล์เสียงไม่เสียหายและเป็นรูปแบบที่รองรับ (MP3, WAV, OGG, FLAC)

### ปัญหา: โปรแกรมช้าหรือค้าง

**วิธีแก้:**
1. ลดจำนวนไฟล์ที่ประมวลผลพร้อมกัน
2. ปิดโปรแกรมอื่นๆ ที่ไม่จำเป็น
3. รอให้โปรแกรมประมวลผลเสร็จ (อาจใช้เวลานานถ้าไฟล์เสียงมีขนาดใหญ่)

### ปัญหา: Error "ModuleNotFoundError: No module named 'pydub'"

**วิธีแก้:**
```bash
pip install pydub
```

หรือ

```bash
pip3 install pydub
```

### ปัญหา: Error เกี่ยวกับ tkinter

**Windows:** ติดตั้ง Python ใหม่และเลือก option "tcl/tk and IDLE"

**Linux:**
```bash
sudo apt install python3-tk
```

**macOS:** tkinter มีมาให้แล้ว ถ้าไม่มี ให้ติดตั้ง Python ผ่าน Homebrew

---

## 🎨 รูปแบบไฟล์เสียงที่รองรับ

- MP3 (.mp3)
- WAV (.wav)
- OGG (.ogg)
- FLAC (.flac)
- M4A (.m4a)
- AAC (.aac)

---

## 💡 ข้อควรรู้

1. **สำรองข้อมูล:** แนะนำให้สำรองไฟล์ต้นฉบับก่อนใช้งานทุกครั้ง
2. **สิทธิ์การเข้าถึง:** ตรวจสอบว่าโปรแกรมมีสิทธิ์เข้าถึงโฟลเดอร์ที่ต้องการใช้งาน
3. **ความเร็ว:** ความเร็วในการประมวลผลขึ้นอยู่กับ:
   - ขนาดไฟล์เสียง
   - จำนวนไฟล์
   - ความเร็วของคอมพิวเตอร์
4. **หน่วยความจำ:** การประมวลผลไฟล์เสียงขนาดใหญ่ต้องการ RAM มาก

---

## 📞 การติดต่อและรายงานปัญหา

หากพบปัญหาหรือต้องการความช่วยเหลือ:
1. ตรวจสอบส่วน "การแก้ปัญหาที่พบบ่อย" ก่อน
2. บันทึก error message ที่ปรากฏ
3. ระบุขั้นตอนที่ทำให้เกิดปัญหา
4. แนบภาพหน้าจอ (ถ้ามี)

---

## 📝 หมายเหตุเพิ่มเติม

- โปรแกรมนี้พัฒนาด้วย Python และใช้ tkinter สำหรับ GUI
- ต้องการ pydub และ FFmpeg สำหรับการประมวลผลเสียง
- รองรับทั้ง Windows, macOS และ Linux
- ฟรีและเป็น Open Source

---

**เวอร์ชัน:** 1.0
**อัพเดทล่าสุด:** พฤศจิกายน 2025
**ภาษา:** Python 3.6+

---

🎵 **ขอให้สนุกกับการจัดการไฟล์เสียง!** 🎵
